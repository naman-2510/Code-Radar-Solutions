#include <stdio.h>
int main() {
    float number;
    printf("%f", number);
    scanf("%f", &number);

    // Printing the floating point number using printf
    printf("%2f\n", number);

    return 0;
}
