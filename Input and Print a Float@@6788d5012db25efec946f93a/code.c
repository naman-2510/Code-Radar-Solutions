#include <stdio.h>
int main() {
    float number;
    printf("%f", number);
    scanf("%f", &number);

    // Printing the floating point number using printf
    printf("%2f", number);

    return 0;
}
